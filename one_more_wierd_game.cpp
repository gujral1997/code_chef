#include <bits/stdc++.h>
using namespace std;
int main(int argc, char const *argv[])
{
	int n;
	cin>>n;
	while(n--)
	{
		int a,b;
		cin>>a>>b;
		bool array[a][b];
		memset(array,false,sizeof(array));
		for(int i=0;i<a;i++)
		{
			for(int j=0;j<b;++j)
			{
				array[i][j]=true;
				if(array[i][j])
			}
		}
	}
	return 0;
}
